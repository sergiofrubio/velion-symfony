{% extends 'includes/header.html.twig' %}

{% block title %}Detalle cita{% endblock %}

{% block main_content %}
    <h1>Cita de {{ appointment.specialization }} </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appointment.id }}</td>
            </tr>
             <tr>
                <th>Patient</th>
                <td>{{ appointment.patient }}</td>
            </tr>
             <tr>
                <th>Therapist</th>
                <td>{{ appointment.therapist }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ appointment.date ? appointment.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ appointment.time ? appointment.time|date('H:i:s') : '' }}</td>
            </tr>
            {# <tr>
                <th>Duration</th>
                <td>{{ appointment.duration }}</td>
            </tr> #}
        </tbody>
    </table>

     <a class="btn btn-secondary" href="{{ path('app_appointment_index') }}">Go back</a>

    <a class="btn btn-warning" href="{{ path('app_appointment_edit', {'id': appointment.id}) }}">Editar</a>

    	<a class="btn btn-danger" href="{{ path('app_appointment_delete', {'id': appointment.id}) }}" onclick="return confirm('¿Deseas elmiminar este elemento?');">
            {# <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash" viewbox="0 0 16 16">
                <path d="M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0z"/>
                <path d="M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1zM4.118 4 4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4zM2.5 3h11V2h-11z"/>
            </svg> #} Eliminar
        </a>

    {{ include('appointment/_delete_form.html.twig') }}
{% endblock %}
